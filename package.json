{
  "name": "meeting-minutes",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check public/app.js && node --check public/audio.js && node --check public/pcm-worklet.js",
    "test:browser": "node scripts/browser-smoke.mjs",
    "test:pages": "node scripts/pages-smoke.mjs"
  }
}
